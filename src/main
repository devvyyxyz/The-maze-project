import pygame
from ui import show_main_menu
from game import start_game

pygame.init()

def main():
    while True:
        choice = show_main_menu()
        if choice == "start":
            start_game()
        elif choice == "quit":
            pygame.quit()
            break

if __name__ == "__main__":
    main()